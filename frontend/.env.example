# Example environment variables for the frontend
# If you run the backend on a different host/port, set VITE_API_URL accordingly.
# Use an empty value to call the same origin (useful with Vite proxy or when serving frontend from the backend).

# VITE_API_URL=http://localhost:5000

# If you rely on Vite dev server proxy, leave this blank and configure proxy in vite.config.js
# VITE_API_URL=
